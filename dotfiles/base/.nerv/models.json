[
  {
    "alias": "qwen3.5-9b-claude-4.6-opus-reasoning-distilled-v2",
    "path": "/Users/josh/.nerv/models/Qwen3.5-9B.Q5_K_M.gguf",
    "hf_repo": "Jackrong/Qwen3.5-9B-Claude-4.6-Opus-Reasoning-Distilled-v2-GGUF",
    "context_length": 32768,
    "gpu_layers": 99,
    "extra_args": [
      // Performance
      "-fa", "on",         // flash attention
      "--mlock",           // keep model in RAM, prevent compression
      "-b", "2048",        // batch size for prompt processing (faster ingestion)
      "-ub", "512",        // physical micro-batch size
      "-t", "7",           // CPU threads (n-1 for M1 Pro 8-core)
      "-tb", "7",          // batch processing threads

      // Server
      "-np", "1",          // single prediction slot (single user)
      "--host", "127.0.0.1",
      "--no-slots",        // don't expose slots endpoint

      // Reasoning (for distilled reasoning models)
      "--reasoning", "on",
      "--jinja",           // use model's chat template

      // KV cache
      "-ctk", "f16",       // KV cache key type
      "-ctv", "f16",       // KV cache value type
      "-nkvo"              // no KV offload (unified memory, nothing to offload to)
    ],
    "port": 1234
  }
]
