#!/bin/sh
case $1 in *playlist*) echo "Use mpdl for playlists." ;; esac

"$HOME/usr/py/.venv/bin/python" -m yt_dlp --ignore-config \
    --remote-components 'ejs:npm' \
    --js-runtimes bun:/opt/homebrew/bin/bun \
    -f 'ba' \
    --add-metadata \
    -o '%(title)s [%(id)s].%(ext)s' "$@"
