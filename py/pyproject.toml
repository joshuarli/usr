[project]
name = "lol"
description = "one venv to rule them all"
version = "0.0.0"
requires-python = "==3.12.6"
dependencies = [
    "yt-dlp",
    "gallery-dl",
    "spotdl",
    "mangadex-downloader",
    # "mangadex-downloader@git+https://github.com/joshuarli/mangadex-dl@v3.0.0",
]

[tool.uv.sources]
mangadex-downloader = { path = "../src/mangadex-dl", editable = true }
