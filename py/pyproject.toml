[project]
name = "lol"
description = "one venv to rule them all"
version = "0.0.0"
requires-python = "==3.13.3"
dependencies = [
    "pip",
    # "yt-dlp",
    "instaloader",
    "browser-cookie3",
    # "spotdl",
    # "mangadex-downloader",
]

[tool.uv.sources]
# yt-dlp = { path = "../src/yt-dlp", editable = true }
# mangadex-downloader = { path = "../src/mangadex-dl", editable = true }
